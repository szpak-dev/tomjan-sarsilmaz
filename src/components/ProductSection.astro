---
export type Props = {
  heading: string;
  divider?: boolean;
};

const { heading, divider = true } = Astro.props;
---

<section class="product-section">
    <h2 class="fs-4 fw-bold mb-0">{heading}</h2>
    {divider && <hr class="text-secondary" />}
    <slot/>
</section>
