---
export type Props = {
    icon: string;
    label: string;
    extraClasses?: string;
}

const { icon, label, extraClasses } = Astro.props;
---
<div class={`d-flex align-items-center gap-2 ${extraClasses || ''}`}>
    <i class={icon}></i>
    <span>{label}</span>
</div>