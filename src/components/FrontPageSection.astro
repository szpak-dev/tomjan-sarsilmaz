---
export type Props = {
  heading: string;
};

const { heading } = Astro.props;
---

<section class="front-page-section py-5">
    <h2 class="fs-4 fw-bold mb-0">{heading}</h2>
    <hr class="text-secondary" />
    <slot/>
</section>
