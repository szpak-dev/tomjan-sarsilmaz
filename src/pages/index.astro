---
import { getRelativeLocaleUrl } from "astro:i18n";
import {defaultLang} from "../i18n/ui";

const redirectUrl = getRelativeLocaleUrl(defaultLang);
---

<meta http-equiv="refresh" content={`0;url=${redirectUrl}`} />
